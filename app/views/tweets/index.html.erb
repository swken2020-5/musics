<% if (flash[:notice]) %>
    <%= flash[:notice]%>
    <br />
<% end %>

  
<%= link_to '作成', '/tweets/new' %>
<hr />
<% @tweets.each do |tweet| %>
 
    <%= tweet.message %>
    

<div class="tweet">
    <audio src= "<%= "get_audio/#{tweet.id}" %>" controls="">
        <a src= "<%= "get_audio/#{tweet.id}" %>"></a>
    </audio>
</div>

    
    <br />
    <%= link_to '表示', "/tweets/#{tweet.id}" %>
    <%= link_to '編集', "/tweets/#{tweet.id}/edit" %>
    <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete %>
    <br />
    <br />
    <%= link_to '戻る', '/' %>
    <br />
<% end %>

 