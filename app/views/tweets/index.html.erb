<% if (flash[:notice]) %>
    <%= flash[:notice]%>
    <br />
<% end %>

  
<%= link_to '作成', '/tweets/new' %>
<hr />
<% @tweets.each do |tweet| %>
    <%= tweet.message %>
    
    <div class="tweet">
      
　　　　<audio audio/webm= "<%= "#{tweet.file}" %>" controls="">
         <a audio/webm="<%= "#{tweet.file}" %>">ダウンロード</a>
       </audio>
　　　 
    </div>

    <br />
   
    <br />

    
    <br />
    <%= link_to '表示', "/tweets/#{tweet.id}" %>
    <%= link_to '編集', "/tweets/#{tweet.id}/edit" %>
    <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete %>
    <br />
    <br />
    <%= link_to '戻る', '/' %>
    <br />
<% end %>

 