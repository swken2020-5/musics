<% if (flash[:notice]) %>
    <%= flash[:notice]%>
    <br />
<% end %>

  
<%= link_to '作成', '/tweets/new' %>
<hr />
<% @tweets.each do |t| %>
    <div class="tweet">
      
　　　　<audio src= "<%= "#{t.file}" %>" controls="">
         <a src="<%= "#{t.file}" %>">ダウンロード</a>
       </audio>
　　　 
    </div>
<% end %>
  
<% @tweets.each do |tweet| %>
    <%= tweet.message %>
    <br />
   
    <br />
    
    <br />
    <%= link_to '表示', "/tweets/#{tweet.id}/show" %>
    <%= link_to '編集', "/tweets/#{tweet.id}/edit" %>
    <%= link_to '削除', "/tweets/#{tweet.id}", method: :delete %>
    <br />
    <br />
    <%= link_to '戻る', '/' %>
    <br />
<% end %>

 